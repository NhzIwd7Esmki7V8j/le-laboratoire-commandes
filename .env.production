# Variables PUBLIQUES inlinées au build (next build) — valeurs non sensibles.
# Nécessaire pour le build cloud Cloudflare Workers Builds, qui n'a pas accès à .env.local.
NEXT_PUBLIC_TRACKING_BOT_USERNAME=labo_num_suivi_bot
