# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
*.tsbuildinfo
*.log

# Cloudflare / OpenNext
.open-next/
.wrangler/
.dev.vars
cloudflare-env.d.ts
cf-secrets.json

# Dev / scratch (non déployé)
dev-telegram-poller.mjs
image-explication/

# Google Drive/Sheets : identifiants OAuth + jeton (SECRETS, jamais sur GitHub)
# Filename sans slash = ignoré dans N'IMPORTE QUEL dossier (racine, rpa/, etc.).
google-oauth.json
google-token.json

# RPA Colissimo : profils de navigateur (cookies/login) + captures/artefacts de test
rpa/.userdata/
rpa/.chromium/
rpa/.inspect/
rpa/.inspect2/
rpa/*.png
rpa/*.pdf